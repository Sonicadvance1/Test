#include "Common.hpp"
class cPlayer
{
	private:
	// Coordinates
	f64 _X, _Y, _Z;
	public:
	
};
namespace Players
{
	// This inserts a player in to the global namespace.
	void InsertPlayer(const u32 ID, cPlayer *Player);
}
